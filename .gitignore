# ===== 模型权重 =====
# 每个 119MB，超过 GitHub 单文件 100MB 硬限制，无法入库。
# 分发方式：网盘 / 服务器 / GitHub Release 附件。
*.pth
# ===== 下面这些都是「项目根目录下」的产物文件夹 =====
# **前导斜杠不能省**：gitignore 里写 `analysis_datas/` 会匹配**任意层级**的同名目录，
# 于是 `tool/analysis_datas/`（工具代码！）也被一起忽略、提交不进去 —— 2026-09-16 实际踩到过。
# 加前导斜杠即锚定到仓库根，只忽略根目录那一份。
/analysis_datas/
/datasets/
/model/
/inference_pictures/
/result/

# ===== 实验报告 =====
# **整个目录都不入库** —— 报告与插图属于研究产物，不进代码仓库。
# （曾经只忽略 figures/、正文和脚本入库；2026-09-20 改为整目录忽略。）
# 前导斜杠锚定到仓库根，不会误伤别处同名的目录。
/experimental_reports/

# ===== Python =====
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# ===== 编辑器 / 本地工具配置 =====
.claude/
.vscode/
.idea/
.DS_Store
Thumbs.db
